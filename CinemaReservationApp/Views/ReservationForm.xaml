<Window x:Class="CinemaReservationApp.Views.ReservationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaReservationApp.Views"
        mc:Ignorable="d"
        Title="ReservationForm" Height="450" Width="800"
        MinHeight="225" MinWidth="400"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Fullname"></Label>
            <TextBox x:Name="fullNameTextBox"></TextBox>
            <Label Content="Email"></Label>
            <TextBox x:Name="emailTextBox"></TextBox>
            <Button x:Name="SubmitButton" Click="SubmitButtonClicked" Height="20" Content="Submit" Margin="0 15 0 0"></Button>
        </StackPanel>
    </Grid>
</Window>
